mutation CreateCharity($options: CharityDataInput!) {
  createCharity(options: $options) {
    errors {
      field
      message
    }
    charity {
      id
      name
      physicalAddress
      postalCode
    }
    success
  }
}
