query AcceptedVolunteers($limit: Int!, $eventIds: [Int!]!) {
  getAcceptedVolunteerRequestListForEvents(
    cursor: null
    limit: $limit
    eventIds: $eventIds
  ) {
    items {
      user {
        ...UserHeader
      }
    }
    hasMore
    errors {
      field
      message
    }
  }
}
