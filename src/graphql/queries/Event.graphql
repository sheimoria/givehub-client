query Event($id: Int!) {
  event(id: $id) {
    id
    name
    createdAt
    description
    dateStart
    dateEnd
    venue
    charity {
      id
      name
    }
    likeNumber
    voteStatus
  }
}
